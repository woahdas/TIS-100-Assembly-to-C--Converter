#include <iostream>
int main() {
int ACC = 0; //accumulator
int BAK = 0; //backup
int NIL = 0; //nil value
if (ACC > 0){
};
return 0;
//evidence that it works (done by converting JGZ (Jump if Greater Than Zero) to CPP using the converter file)
}
